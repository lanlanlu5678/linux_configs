" if ~/.vimrc exists, runtime command will only search files in ~/.vim
"runtime! archlinux.vim



" Common
if &compatible
	set nocompatible
endif


" Syntax Highlight
syntax on


" Apperance
"	line numbers
set number
"	indent
set tabstop=4
set autoindent
set shiftwidth=4
set softtabstop=4
